The prefered way of contributing is through pull requests. Make sure to add
tests aswell. We use travis sa CI, which will verify all pull requests. You
can also run the tests local using docker with:

```
make test
```
